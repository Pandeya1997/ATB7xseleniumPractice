package com.thetestingacadmey.ex_selenium;

public class Lab299 {
    public static void main(String[] args) {
        //  SearchContext driver = new EdgeDriver(); // Dynamic Dispatch (RunTime Polymorphism)
        // possible but two function, which is not much used.
        //WebDriver driver = new EdgeDriver();
        //b12 which are good]

        // EdgeDriver driver = new EdgeDriver();

        // scenarios
        //  1. Do want to run on chrome or edge
        //  ChromeDriver driver1 = new ChromeDriver();
        //  EdgeDriver driver2 = new EdgeDriver();

// 2. Do you want to run on chrome then change to edge?
        //   WebDriver driver = new ChromeDriver();
        //  driver = new EdgeDriver();
        // 3. do you want to eun on multiple browser
        //  RemoteWebDriver driver (with GRID) - Advance (Last 2 session )


    }
}
